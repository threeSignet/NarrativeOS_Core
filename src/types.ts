// =============================================================================
// 类型系统统一入口 — 重导出 types/ 目录下的所有子模块
// =============================================================================
// 这是一个薄重导出文件，实际定义在 types/ 目录中按领域拆分。
// 所有现有代码可继续使用 `from '../types.js'` 导入。
// 新代码推荐按领域直接导入子模块（如 `from '../types/entity.js'`）。
// =============================================================================

export * from './types/index.js';
